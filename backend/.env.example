# Variables d'environnement — Backend Gestion Stock
# Copiez ce fichier en .env et remplissez les valeurs

MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/gestion-stock?retryWrites=true&w=majority
PORT=5000
